# Scripts

Utility scripts for data import, synchronization, migration, deployment, and local diagnostics.

## Folders

- `debug/`: one-off inspection and verification scripts used while checking data issues.
- `deploy/`: deployment helper scripts.
- `env/`: environment setup helper scripts.

Keep new one-off checks inside `debug/` so the project root stays focused on app configuration.
